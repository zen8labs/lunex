name: 'publish'

on:
  push:
    tags:
      - '*'

jobs:
  publish-tauri:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest' # for Arm based macs (M1 and above).
            args: '--target aarch64-apple-darwin'
          - platform: 'macos-latest' # for Intel based macs.
            args: '--target x86_64-apple-darwin'
          - platform: 'ubuntu-22.04'
            args: ''
          # - platform: 'ubuntu-22.04-arm' # Only available in public repos.
          #   args: ''
          - platform: 'windows-latest'
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04' # This must match the platform value defined above.
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'yarn' # Set this to npm, yarn or pnpm.

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable # Set this to dtolnay/rust-toolchain@nightly
        with:
          # Those targets are only used on macos runners so it's in an `if` to slightly speed up windows and linux builds.
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: install frontend dependencies
        # If you don't have `beforeBuildCommand` configured you may want to build your frontend here too.
        run: yarn install # change this to npm or pnpm depending on which one you use.

      - name: Set up Sentry DSN
        # Set Sentry DSN as environment variables for build
        # These will be embedded into the binary at compile time
        run: |
          echo "VITE_SENTRY_DSN=${{ vars.SENTRY_DSN }}" >> $GITHUB_ENV
          echo "RUST_SENTRY_DSN=${{ vars.SENTRY_DSN }}" >> $GITHUB_ENV

      - uses: tauri-apps/tauri-action@dev
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Sentry DSN - embedded at build time
          # Use repository variable SENTRY_DSN for both frontend and backend
          VITE_SENTRY_DSN: ${{ vars.SENTRY_DSN }}
          RUST_SENTRY_DSN: ${{ vars.SENTRY_DSN }}
          # APPLE_ID: ${{ secrets.APPLE_ID }}
          # APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          # APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          # APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          # APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          # APPLE_SIGNING_IDENTITY: ${{ env.CERT_ID }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: __VERSION__ # the action automatically replaces \_\_VERSION\_\_ with the app version.
          releaseName: 'Nexo __VERSION__'
          releaseBody: |
            # 🎉 Nexo v0.1.0-beta.1 - First Beta Release!

            Chào mừng đến với phiên bản Beta đầu tiên của Nexo! Sau 52 phiên bản alpha, chúng tôi tự hào giới thiệu một sản phẩm ổn định và đầy đủ tính năng, sẵn sàng cho người dùng thử nghiệm rộng rãi.

            ## ✨ Điểm nổi bật

            ### 🤖 Hỗ trợ đa LLM toàn diện
            - Kết nối với tất cả các nhà cung cấp AI hàng đầu: OpenAI, Anthropic, Google Gemini, Ollama, LM Studio, và nhiều hơn nữa
            - Tự động cấu hình URL mặc định thông minh
            - Quản lý nhiều kết nối đồng thời

            ### 🔌 Tích hợp MCP (Model Context Protocol)
            - Hỗ trợ đầy đủ MCP servers và tools
            - Mở rộng khả năng của AI với các công cụ bên ngoài

            ### 📝 Trình chỉnh sửa Flow mạnh mẽ
            - Tạo workflow trực quan với drag-and-drop
            - Node Groups với tùy chỉnh màu sắc và độ trong suốt
            - Property panel thông minh

            ### 🌐 Đa ngôn ngữ (i18n)
            - Hỗ trợ Tiếng Việt và English
            - Tự động phát hiện ngôn ngữ hệ thống

            ### 🎨 Giao diện Premium
            - Thiết kế hiện đại với dark mode
            - First-run setup được làm mới hoàn toàn
            - Animations mượt mà và responsive

            ### 🔐 Bảo mật \u0026 Riêng tư
            - Dữ liệu lưu trữ 100% local
            - Không thu thập thông tin người dùng
            - Mã nguồn mở, minh bạch

            ## 🐛 Sửa lỗi gần đây
            - Cải thiện hiển thị runtime trong Addon Settings
            - Sửa lỗi chat input model list
            - Các cải tiến UI/UX khác

            ---

            **Lưu ý**: Đây là phiên bản Beta, có thể còn một số lỗi chưa được phát hiện. Vui lòng báo cáo issues tại: https://github.com/Nexo-Agent/nexo/issues

            Cảm ơn bạn đã sử dụng Nexo! 🙏
          releaseDraft: false
          prerelease: true
          args: ${{ matrix.args }}
