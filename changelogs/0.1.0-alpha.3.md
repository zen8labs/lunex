# Changelog 0.1.0-alpha.3

Build: `2026-01-04`

## 🚀 New Features

- **Usage Tracking**: Implemented comprehensive usage tracking with dedicated database, backend services, and frontend UI in settings (`b0849ef`).
- **Workspace Settings Page**: Extracted workspace settings into a dedicated page with updated navigation and UI state management (`2a609b4`).
- **Error Boundary**: Added global UI error boundary with categorized notifications and standardized Tauri command error handling (`15c096d`).
- **Python Runtime Management**: Bundled `uv` sidecar to manage Python runtime installations (`f69bf6a`).
- **Generic Tool Installation**: Abstracted sidecar binary bundling with a generic tool installation mechanism for `uv` and `fnm` (`dc4681e`).
- **Agent Behavioral Rules**: Added agent behavioral rules for language usage (`be89978`).

## ♻️ Refactoring

- **Navigation Architecture**: Refactored chat and settings into dedicated pages, removing dialog-based settings (`4856912`).
- **Error Handling**: Unified error handling using a custom `AppError` type across commands and services (`213f2c6`).
- **Message Action Buttons**: Relocated message action buttons outside the message bubble and updated their styling and icon sizes (`64edfab`).

## 🐛 Bug Fixes

- **Runtime UI**: Ensured consistent spacing for runtime path display by always reserving its height (`000ee7d`).

## 📝 Documentation

- **Error Handling Guidelines**: Added frontend error handling guidelines and updated backend error handling to use `AppError` and `Result<T, AppError>` (`20d7127`).

## 🧹 Chores

- **Code Cleanup**: Removed workspace settings section and its Briefcase icon import (`3ff6bf1`).
